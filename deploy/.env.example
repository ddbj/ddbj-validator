# production / staging
DDBJ_ENV=staging

# ---- 環境の分離（同じ main を両ホストで使うため、名前は .env だけで決める）----
# compose project 名。コンテナは <project>_<service>_1 で命名されるので web は
# ddbj-validator-staging_web_1 / ddbj-validator-production_web_1 になる。
# 未設定なら podman-compose 既定（=ディレクトリ名 deploy）。
#   staging(a012)=ddbj-validator-staging / production(a011)=ddbj-validator-production
DDBJ_COMPOSE_PROJECT=ddbj-validator-staging
# validator コンテナ名（compose の container_name）。project 名と同じ値にそろえる
# （validator=<name>、web=<name>_web_1 となり、環境ごとに 1 つの名前だけ覚えればよい）。
#   staging(a012)=ddbj-validator-staging / production(a011)=ddbj-validator-production
# 下の DDBJ_VALIDATOR_CMD の `podman exec <name>` と必ず一致させること
# （ずれると web が存在しないコンテナを exec して全検証が失敗する。compose.sh が起動時に検査する）。
DDBJ_VALIDATOR_NAME=ddbj-validator-staging
# 本番a011=13101 / ステージングa012=13102（環境ごとに +1）。
# 10000番以下は ufw でブロックされる可能性があるため 1万番台を使う。
DDBJ_WEB_PORT=
# 公開バインド先 host IP。インターネット非公開・D-way すぱこん区画のみアクセス可にするため
# 全IF(0.0.0.0)ではなく特定IFに限定する。未設定なら全IF公開。
# a012 の正名(canonical)は InfiniBand ibp129s0f0=172.19.15.12（内部DB 172.19.15.11 と同一
# 172.19.15.x 網＝D-way アクセス網）。※172.19.27.12 は br0=別名 a012a なので使わない。
DDBJ_BIND_HOST=
DDBJ_DATA_DIR_HOST=/data1/ddbj-validator/biosample/logs/staging

# 別コンテナ運用（推奨・本番）: web が validator コンテナを podman exec で呼ぶ。
# validator を頻繁に更新しても web を止めずに入れ替えられる（無停止）。
# 有効化するには下記 2 つを設定し、compose の web volumes の socket 行のコメントを外す。
# 空にすると単一コンテナ運用（web 内で子プロセス実行）。コンテナ名は DDBJ_VALIDATOR_NAME と一致させる。
DDBJ_VALIDATOR_CMD=podman exec ddbj-validator-staging ddbj-validator
# host の rootless podman socket パス（`podman info --format '{{.Host.RemoteSocket.Path}}'` で確認）
DDBJ_PODMAN_SOCK=/run/user/1000/podman/podman.sock

# 検証モードは web api では内部 DB 固定（-d）。mode 指定は無い（local/ncbi はあり得ない）。
# 内部 DB 接続情報（PGHOST 等）は **リポジトリ直下の .env** に置き、validator がそこから読む
# （compose は validator の env_file に ../.env を追加済み）。deploy/.env には DB 接続情報・NCBI キーを書かない。
